import React from 'react'
import { Input } from '@chakra-ui/react'
const SearchBar = () => {
    return <Input placeholder="Search Users" py={'20px'} variant={'filled'} />
}

export default SearchBar
